# ================================
# Packages to install separately first (compiled binaries)
# ================================
# Install these one by one before the rest:
# pip install numpy==1.26.3 --only-binary=:all:
# pip install Pillow==11.3.0
# pip install opencv-python==4.9.0.80
# pip install scikit-learn==1.6.1 --only-binary=:all:

numpy==1.26.3
Pillow==11.3.0
opencv-python==4.9.0.80
scikit-learn==1.6.1

# ================================
# Other dependencies (can be installed together)
# ================================
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
httpx==0.26.0
python-multipart==0.0.6
pytesseract==0.3.10
deepface==0.0.75
google-genai==0.2.2
